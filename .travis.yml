# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
R:
  - oldrel
  - release
  - devel

cache: packages


apt_packages:
  - libxml2-dev

script:
- R CMD build .
- R CMD check *tar.gz
